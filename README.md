# Create a new repository on the command line
```bash
echo "# Git_Test" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/noisewurt-design/Git_Test.git
git push -u origin main
```